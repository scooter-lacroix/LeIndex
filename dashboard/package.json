{
  "name": "leindex-dashboard",
  "private": true,
  "version": "1.5.0",
  "description": "LeIndex Dashboard - Visual interface for code indexing and analysis",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "build": "bun build ./index.html --outdir=dist --minify",
    "start": "bun run server.ts",
    "preview": "bun run server.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not configured'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "bun-types": "latest",
    "typescript": "^5.3.0"
  }
}
